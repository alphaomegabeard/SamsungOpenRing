<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Debug builds used to auto-start the developer-only Claude Bridge service.
      That service can start before runtime Bluetooth/notification permissions are
      granted and crash on newer Android versions. Keep the debug source set, but
      do not register or auto-start the bridge in installable builds.
    -->

</manifest>
